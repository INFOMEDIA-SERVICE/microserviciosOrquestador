spring:
  application:
    name: signatures-server

  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 20
        multiplier: 1.3
        max-interval: 2000
      uri: http://localhost:8888
