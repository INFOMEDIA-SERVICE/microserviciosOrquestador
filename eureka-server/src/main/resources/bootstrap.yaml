server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
spring:
  application:
    name: infomediaservice_eureka-application

  main:
    allow-bean-definition-overriding: true

    #profiles:
    #active: "@profile.active@"

  #cloud:
    #config:
      #enabled: true
      #fail-fast: true
        #retry:
        #initial-interval: 1000
        #max-attempts: 20
        #multiplier: 1.3
      #max-interval: 2000

      #uri: http://localhost:8888 #"@config.server.url@"
      #username: "@config.server.user.name@"
      #password: "@config.server.user.pass@"
---

#spring:
#profiles: local, development-docker

#boot:
#admin:
#client:
#url: "@admin.server.url@"
#username: "@admin.server.user.name@"
#password: "@admin.server.user.pass@"

#cloud:
# kubernetes:
#  enabled: false
# ribbon:
